# launch-before-terminate 검증 (2026-09-01 15:49~16:08, #112)

조건: fixed 50세션 / SESSION_SEC 960 / PROBE_SEC 2, main 병합이 트리거한 실제 배포(#112 설정 포함)를
k6로 관통. 알람은 **켜둔 채** 측정(오탐이 안 울리는 것이 검증 항목).

## 판정: 성공

| 항목 | before (#107까지, 3회 배포) | after (#112) |
|---|---|---|
| 배치 내 순서 | Terminating이 Launching보다 **1.6초 먼저** | **Launching이 5분 27초 먼저** (헬시+워밍업 후 종료) |
| 드레인 소켓의 재접속 분배 | 남은 1대로 전량 몰림 | **반반 분산** — 50개→25/25, 75개→38/37 (활성 연결 그래프 실측) |
| 이중 탈출 | 50 | **25** (1012 총 150→125) |
| 실질 1대 서빙 구간 | ~2분 | **없음** (항상 헬시 ≥2) |
| 배포 중 UnHealthyHost 알람 | 매번 오탐 | **무음** |
| 복원 / 세션 | 100% / 100% | **167/167 / 50/50** |

활동 기록: 15:48:43 Launch 새#1 → 15:54:10 Term 옛#2 → 15:54:11 Launch 새#2 → 16:00:00 Term 옛#1.
수정은 refresh preferences의 `MaxHealthyPercentage: 150` 한 줄 (150% of desired 2 = 3대 = ASG MaxSize).

## 계획에 없던 보너스 — 클라이언트 측 네트워크 순단의 흡수

16:03:05±1s에 42개 소켓이 **코드 없이** 끊겼다(1012 125 + 무코드 42 = 총 167). 원인 분해:
- 서버는 이 끊김을 보지 못했다 — 활성 연결 합계가 100→**142**로 부풂(죽은 42가 서버 맵에 유령으로 잔류)
  → 절단 지점은 서버·ALB가 아니라 **측정 노트북~ALB 사이**(Wi-Fi/NAT 순단)
- 90초 뒤 **하트비트 리퍼가 유령 42개를 1001로 정리** (142→100 수축, 종료 코드 패널의 1001)
- 42건 전부 backoff 재접속으로 복원(계약의 "코드 없는 종료 → 지수 backoff" 경로) — 복원 꼬리
  p90 11.3s와 relay max 8.4s(TCP 재전송 지연)는 이 순단의 흔적
- 즉 재연결 계약 + 리퍼가 **계획에 없던 실제 장애까지 흡수**해 세션 손실 0

## 남은 관측 한계 (기지)

- 드레인한 인스턴스의 서버 측 1012 카운터는 인스턴스 종료와 함께 대부분 소실 — 클라(k6)가 진실의 원천.
  후속 후보: 로그 수집(CloudWatch Logs) 또는 종료 전 지표 push
- 2대 롤링에서 이중 탈출 25는 바닥값(0으로 만들려면 블루/그린)
