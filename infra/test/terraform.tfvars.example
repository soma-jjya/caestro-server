# terraform.tfvars 로 복사 후 값 채우기 (terraform.tfvars 는 커밋 금지)

# TURN 비밀번호 (영문+숫자 권장, 특수문자 피하기)
turn_password = "caestroTurn2026"

# app.jar scp / SSH 용 키페어 이름 (AWS 콘솔 EC2 > 키페어에서 미리 생성) - 필수
key_name = "여기에_키페어_이름"

# 보안상 본인 공인 IP로 제한 권장
# ssh_cidr = "1.2.3.4/32"
